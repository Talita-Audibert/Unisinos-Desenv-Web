		<div class="col-lg-12">
			<h1 class="centralized">Lista de carros reservados por você</h1>
			<table class="table table-hover" id="reserves">
				<tbody>
					<tr>
						<th>Carro</th>
						<th>Periodo Inicial</th>
						<th>Periodo Final</th>
						<th>Ações</th>
					</tr>
					{{#reservas}}
					<tr>
						<td>{{carro}}</td>
						<td>{{periodo_inicial}}</td>
						<td>{{periodo_final}}</td>
						<td><a class="btn btn-danger" href="/painel/reservas?id={{id}}">remover</a></td>
					</tr>
					{{/reservas}}
				</tbody>
			</table>
		</div>